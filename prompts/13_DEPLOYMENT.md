# Prompt 13 - Deployment

Read `docs/DEPLOYMENT.md`.

Prepare deployment configuration for:
- Cloudflare edge;
- Rust API service;
- Rust worker;
- Neon PostgreSQL;
- Cloudflare R2;
- optional Cloudflare Queue integration through a transport adapter.

Add staging/production environment configuration templates and smoke checks.

Do not commit secrets.
